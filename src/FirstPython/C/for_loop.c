#include <stdio.h>
int main (){
    int i, j;
    for (i=2 ; i<=4; i++){ // i<=4가 true, i++가 false일 경우 나오는 반환값.
        for (j=5; j<=7; j++){
        }
    }
    printf("%d x %d = %2d", j, i, i* j);
}